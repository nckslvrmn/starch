#!/bin/bash
# Vendored from shahnawazshahin/steam-using-gamescope-guide with the path fixed.
# Upstream exec'd /usr/bin/steamos-update, which doesn't exist here.
set -eu
exec /usr/local/bin/steamos-update
