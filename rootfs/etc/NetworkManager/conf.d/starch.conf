# NM owns IP configuration for every interface. Steam's gamepadui network panel
# reads NM over D-Bus, so anything NM doesn't manage is invisible there — a
# wired link would show as "no network" while routing fine. iwd remains the
# WiFi backend (see iwd-backend.conf), so iwctl still works for inspection.

[main]
plugins=keyfile
dns=systemd-resolved
