# NM owns IP configuration on every interface. Steam's gamepadui network panel
# reads NM over D-Bus, so anything NM doesn't manage is invisible there: a wired
# link shows as "no network" while routing fine. iwd is still the WiFi backend
# (see iwd-backend.conf), so iwctl works for inspection.

[main]
plugins=keyfile
dns=systemd-resolved
