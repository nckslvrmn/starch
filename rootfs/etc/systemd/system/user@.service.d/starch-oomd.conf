# Managed by starch. Kill the most memory-pressured cgroup inside the user
# session (where games live) when pressure stays above the limit — a leaking
# game dies, the session survives.
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=80%
