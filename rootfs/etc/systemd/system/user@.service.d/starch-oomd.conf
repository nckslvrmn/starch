# Managed by starch. When pressure stays above the limit, kill the worst cgroup
# inside the user session, where the games are. The leaking game dies and the
# session survives.
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=80%
