# Console behavior for the power button: tap = sleep (NVIDIA VRAM is
# preserved across suspend; see modprobe.d/starch-nvidia.conf), hold = off.
# systemd's default tap action is an immediate poweroff — brutal mid-game.
[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff
