# Power button: tap sleeps, hold powers off. systemd's default tap action is an
# immediate poweroff, which is brutal mid-game. Suspend is safe here because
# modprobe.d/starch-nvidia.conf preserves VRAM.
[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff
