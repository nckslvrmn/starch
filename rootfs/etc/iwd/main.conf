# Managed by starch. iwd handles association only, NetworkManager owns IP.
# Leaving EnableNetworkConfiguration on gives the interface two DHCP clients.
[General]
EnableNetworkConfiguration=false

[Network]
PreferredBand=5
