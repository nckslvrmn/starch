# Managed by starch. iwd owns WiFi end-to-end: association + DHCP.
# NetworkManager talks to it as a backend (wifi.backend=iwd) but does not
# manage IP config.
[General]
EnableNetworkConfiguration=true

[Network]
PreferredBand=5
