# Lets the Steam session flip the perf preset without prompting. Scoped to one
# binary; @@GAMING_USER@@ is substituted at install time.
@@GAMING_USER@@ ALL=(root) NOPASSWD: /usr/local/bin/starch-perf-mode
