# i915 loads early so it (the display controller on Optimus) wins the
# fbcon/plymouth handoff. NVIDIA modules also load early so PRIME-offloaded
# clients can open the render node without racing modprobe.
MODULES=(i915 nvidia nvidia_modeset nvidia_uvm nvidia_drm)
