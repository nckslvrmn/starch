# Managed by starch-select-display.
# Re-generate by running: starch-select-display {auto|internal|external}
#
# This file controls which outputs the SDDM greeter (weston) renders on.
# Outputs listed with `mode=off` will be left dark by the greeter compositor,
# so SDDM only appears on the remaining outputs.
