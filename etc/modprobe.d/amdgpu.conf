# Modern AMD (Polaris / Vega / RDNA1+) needs no options — amdgpu auto-claims.
# Uncomment only if you need them:
#   options amdgpu si_support=1 cik_support=1      # older GCN (SI/CI)
#   options amdgpu ppfeaturemask=0xffffffff        # expose pp_* sysfs knobs
